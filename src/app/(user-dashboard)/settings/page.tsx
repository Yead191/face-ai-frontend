import Settings from '@/feature/user-dashboard/settings';
import React from 'react';

const page = () => {
    return (
        <div>
           <Settings />
        </div>
    );
};

export default page;