import Progress from '@/feature/user-dashboard/progress';
import React from 'react';

const page = () => {
    return (
        <div>
            <Progress />
        </div>
    );
};

export default page;